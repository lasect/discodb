[package]
name = "discodb-catalog"
version = "0.1.0"
edition = "2024"

[dependencies]
discodb-types = { path = "../types" }
discodb-observability = { path = "../observability" }
serde = { version = "1.0", features = ["derive"] }
thiserror = "2.0"
ahash = "0.7"
