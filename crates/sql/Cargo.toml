[package]
name = "discodb-sql"
version = "0.1.0"
edition = "2024"

[dependencies]
discodb-types = { path = "../types" }
discodb-observability = { path = "../observability" }
discodb-catalog = { path = "../catalog" }
discodb-executor = { path = "../executor" }
thiserror = "2.0"
